name: Release
on:
  push:
    branches:
      - master

jobs:
  release:
    uses: centralnicgroup-opensource/rtldev-middleware-shareable-workflows/.github/workflows/semantic-notify-release.yml@main
    secrets: inherit